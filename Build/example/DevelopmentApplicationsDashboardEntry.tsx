import React from 'react';
import ReactDOM from 'react-dom/client';
import DevelopmentApplicationsDashboard from '../src/examples/DevelopmentApplicationsDashboard';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <DevelopmentApplicationsDashboard />
  </React.StrictMode>
);