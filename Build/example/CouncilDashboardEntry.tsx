import React from 'react';
import ReactDOM from 'react-dom/client';
import CouncilDashboard from '../src/pages/CouncilDashboard';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <CouncilDashboard />
  </React.StrictMode>
);