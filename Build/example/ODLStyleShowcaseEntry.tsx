import React from 'react';
import ReactDOM from 'react-dom/client';
import ODLStyleShowcase from '../src/pages/ODLStyleShowcase';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <ODLStyleShowcase />
  </React.StrictMode>
);