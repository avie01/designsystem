import React from 'react';
import ReactDOM from 'react-dom/client';
import DropdownDemo from '../src/pages/DropdownDemo';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <DropdownDemo />
  </React.StrictMode>
);