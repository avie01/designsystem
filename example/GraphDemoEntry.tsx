import React from 'react';
import ReactDOM from 'react-dom/client';
import GraphDemo from '../src/pages/GraphDemo';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <GraphDemo />
  </React.StrictMode>
);